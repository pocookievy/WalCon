name: JavaScript Syntax Check

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check app-v6.js
        run: node --check app-v6.js
      - name: Check boot-v7.js
        run: node --check boot-v7.js
